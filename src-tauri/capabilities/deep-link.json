{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "deep-link",
  "description": "Deep links are handled by Rust and forwarded as events; renderer deep-link command permissions are not exposed.",
  "windows": ["main"],
  "permissions": []
}
