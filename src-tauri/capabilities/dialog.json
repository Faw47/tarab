{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog",
  "description": "Native dialog permissions",
  "windows": ["main"],
  "permissions": [
    "dialog:default",
    "dialog:allow-open"
  ]
}
