{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog",
  "description": "Native open-dialog permissions for main-window folder and audio selection.",
  "windows": ["main"],
  "permissions": ["dialog:allow-open"]
}
