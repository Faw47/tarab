{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog",
  "description": "Native open-dialog permissions for main-window folder, audio, and image selection.",
  "windows": ["main"],
  "permissions": ["dialog:allow-open"]
}
