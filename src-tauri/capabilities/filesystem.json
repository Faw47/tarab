{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "filesystem",
  "description": "Renderer filesystem permissions are limited to watching user-selected music folders. Reads and mutations go through Rust commands with path validation.",
  "windows": ["main"],
  "permissions": [
    "fs:allow-watch",
    "fs:allow-unwatch",
    "fs:scope-desktop-recursive",
    "fs:scope-document-recursive",
    "fs:scope-download-recursive",
    "fs:scope-audio-recursive",
    "fs:scope-appdata-recursive"
  ]
}
