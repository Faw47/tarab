{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "notifications",
  "description": "Permissions for sending notifications",
  "windows": ["main"],
  "permissions": [
    "notification:default",
    "notification:allow-notify"
  ]
}
