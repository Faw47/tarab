{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "opener",
  "description": "Reveal-in-folder is main-window only; the mini player sends playback intents and must not open files or URLs.",
  "windows": ["main"],
  "permissions": ["opener:allow-reveal-item-in-dir"]
}
