{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "opener",
  "description": "The main window may open the reviewed help URL; native commands own all filesystem reveals.",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "https://github.com/Faw47/tarab#readme" }]
    }
  ]
}
