{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "process",
  "description": "Permissions for process management (macOS only; tauri-plugin-process is not linked on Windows)",
  "windows": ["main"],
  "platforms": ["macOS"],
  "permissions": [
    "process:default",
    "process:allow-restart"
  ]
}
