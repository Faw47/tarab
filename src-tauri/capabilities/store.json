{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "store",
  "description": "Main-window player session persistence for tarab-player.dat only.",
  "windows": ["main"],
  "permissions": [
    "store:allow-load",
    "store:allow-get-store",
    "store:allow-get",
    "store:allow-set",
    "store:allow-save"
  ]
}
