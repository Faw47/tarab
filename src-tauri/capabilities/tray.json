{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray",
  "description": "Status icon and tray menu are created from Rust; renderer tray mutation APIs are not exposed.",
  "windows": ["main"],
  "permissions": []
}
